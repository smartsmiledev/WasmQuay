# wasmquay capability policy — networked edge function
#
# An edge worker that is allowed to reach the network and read a clock,
# but must never touch the local filesystem or spawn unknown host calls.

policy "edge-networked"
default deny
